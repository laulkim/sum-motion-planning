# 작업 관리

최종 갱신: 2026-09-20

## 기록 원칙

- 이 저장소의 작업을 시작할 때 이 문서를 확인하고 목표와 할 일을 적는다.
- 작업 중에는 진행 상황, 결정 사항, 막힌 이유를 갱신한다.
- 완료 시 변경 파일과 검증 결과를 기록하고 남은 일을 정리한다.
- 확인된 할 일과 검토 제안을 구분한다. 실행하지 않은 검증은 완료로 표시하지 않는다.
- 구현은 기존 코드와 기능을 우선 활용하고 필요한 범위만 변경한다.

## 진행 중

- 없음. 독립 Python 패키지 `simp_tracker` 구현·빌드·제어식/ROS 통합 검증 완료.
- 사용자 추가 지시에 따라 논문 4.1절의 오차식·제어식·리아프노프 함수만 중심으로 적용. 차륜 구조와 이벤트 트리거 방식은 제외한다.

## 해야 할 일

- 후속 검토: 실제 주행 조건별 gain 조정, 보정 명령의 가속도·저크·충돌 여유 제한, 플래너의 실측 odometry 기반 재계획 정책.
- 기존 시각화 회귀 테스트 3건 실패 및 기존 Python 패키지의 colcon 테스트 탐색 문제는 이번 범위 외 미해결 항목으로 기록한다(아래 검증 내역 참고).

## 현재 구현 결정 (2026-09-20)

- 사용자 요청에 따라 점 메시지 `TrajectoryPoint.msg`와 배열 메시지 `Trajectory.msg`를 추가한다. 아래 설계 논의는 결정 이전 기록이다.
- `Trajectory`: 발행 시각/고정 좌표계 header, plan_id, ROS 실행 시작 시각 start_time, points 배열.
- `TrajectoryPoint`: x/y/차체 yaw, 목표 차체 축 기준 vx/vy/yaw_rate, 초 단위 float64 time_from_start, 주행 모드.
- `/planner/trajectory`에 최종 AllocationResult의 모든 샘플을 재샘플링 없이 발행한다. 취소 가능한 pending 계획은 발행하지 않고 기존 계획 활성화 시점에 발행한다.
- 통합 launch는 기본적으로 Planner `/cmd_vel`을 `/planner/cmd_vel`로 remap하고 `simp_tracker`만 최종 `/cmd_vel`을 발행한다. `use_tracking_controller:=false`로 기존 직접 연결을 선택할 수 있다.
- `ExecutedCommand.execution_state`와 plan_id를 하나의 heartbeat로 전달한다. 정상 계획은 추종 제어, 안전정지·모드 정지·제자리 회전은 기존 nominal 명령 전달로 처리한다. transient-local 궤적 수신만으로 실행하지 않는다.
- Controller는 최신 odometry를 사용한다. 플래너는 기존의 계획 예측 기반 상태 갱신을 유지한다. 명령이 끊기면 Controller는 0 속도, Controller 자체가 중단되면 Simulator는 0.5초 timeout 후 정지한다.
- 메시지 변환은 `planner_node.cpp::publish_trajectory()`에서 직접 수행한다. 별도 변환 헤더 없이 실제 ROS 토픽 확인 스크립트로 발행을 검증한다.

## 설계 논의 기록 — 궤적추종 제어기 (2026-09-20)

아래는 구현 전 논의 기록이다. 최신 결정과 구현 결과는 위 항목과 완료 기록을 따른다.

### 제어기 패키지 배치 방향 — 별도 패키지

- 사용자가 역할별 분리를 지적함에 따라 기존 패키지에 추가하자는 제안을 수정한다. Simulator가 `planar_velocity_sim`으로 분리된 현재 구조에 맞춰 Controller도 별도 패키지로 둔다.
- 패키지명은 `simp_tracker`를 제안한다. Planner는 궤적 생성, Controller는 궤적 추종과 속도 명령 생성, Simulator는 차량 상태 갱신을 맡는다.
- 기존 `simp_planner_msgs`의 Trajectory를 입력으로 사용하며 플래너 core 라이브러리에 의존하지 않는다. `/odom`은 상태 피드백, `/cmd_vel`은 향후 Controller 출력으로 연결한다.
- 초기 구성은 package.xml, CMakeLists.txt, 제어기 노드 소스와 필요한 검증으로 제한한다. 별도 프레임워크나 공용 라이브러리는 미리 만들지 않는다.
- 현재 요청은 배치 논의이며 Controller 패키지/코드는 아직 추가하지 않는다.

### 코드에서 확인한 사실

- 플래너가 `/cmd_vel` (`Twist`)을 직접 발행하고 시뮬레이터가 차체 속도를 적분한다.
- `/planner/selected_trajectory_data`는 `ReferencePath`의 위치·이동 방향·모드를 발행하며, 시간별 속도와 차체 자세를 담지 않는다.
- `/planner/executed_command`는 플래너가 생성한 명령 기록이며 시뮬레이터의 실제 적용 결과가 아니다.
- 플래너 내부 `AllocationResult`에는 시간 궤적과 차체 yaw(`psi`), `vx`, `vy`, `yaw_rate` 등이 존재한다.
- `odom_callback()`은 최초 odometry만 반영한다. 이후 계획 시작 상태는 `predict_handover_state()`의 예측으로 갱신한다.
- 플래너가 계획 활성화, 안전정지, 모드 전환 및 제자리 회전 실행도 담당하므로 제어기 분리 시 이 경로들을 함께 검토해야 한다.

### 제안 — 미확정

- 첫 단계는 현재 시뮬레이터에 맞춰 차체 속도 기반 추종 제어기로 구성한다.
- 플래너 입력용 `ReferencePath`는 유지하고, 제어기에는 계획 ID·시작 시각·시간별 목표 위치/차체 yaw/차체 속도를 담는 별도 궤적 메시지를 제공한다.
- 제어기 출력은 기존 `Twist`를 재사용할 수 있다. 명령 시각·좌표계가 필요하면 `TwistStamped`로 시뮬레이터 구독부까지 함께 변경한다.
- `/odom`, `DriveModeState`를 제어기 피드백으로 활용하고, 구동 명령 발행 주체를 하나로 정한다.
- 재계획을 실제 상태에 연동할지와 계획 기준 상태를 유지할지 명시적으로 결정한다. 실제 상태와 미래 예측 상태는 구분한다.
- 궤적 교체·만료, 모드 정렬 중 동작, 정지/제자리 회전 표현과 속도·가속도 제한을 메시지 계약과 함께 정의한다.
- 검증 후보: 초기 위치/자세 오차, 횡이동·후진, 계획 교체, 궤적/명령 끊김, 모드 전환 및 제자리 회전.

### 메시지 재사용 여부 논의 — 미확정

- 권고: 플래너→제어기는 별도 `TrackingTrajectory` 메시지와 `TrackingTrajectoryPoint` 점 타입을 정의한다. 이름은 가안이다.
- 최소 정보: 계획 ID, 좌표계, 실행 시작 시각, 점별 상대 시간·위치·차체 yaw·차체 기준 vx/vy/yaw_rate·필요 주행 모드.
- 기존 `Twist`는 단일 속도 명령이므로 목표 위치·자세·미래 시간 궤적을 전달하지 못한다. 사용하지 않는 축에 다른 의미의 값을 넣지 않는다.
- `ReferencePath`와 현재 발행하는 `nav_msgs/Path`는 추종에 필요한 시간별 속도·차체 자세 계약이 부족하다. `ExecutedCommand`는 현재 실행 명령 기록 용도이므로 미래 목표 궤적 용도로 재사용하지 않는 안을 권고한다.
- 단순 속도 명령 전달만 필요하면 기존 `Twist`로 충분하다. 이번 권고는 위치·차체 자세 오차를 보정하는 시간 궤적 추종을 전제로 한다.
- 제어기→현재 시뮬레이터 출력은 기존 `Twist`, 피드백은 `/odom` 및 `DriveModeState`를 재사용하는 안을 유지한다.
- 메시지 파일 생성은 아직 하지 않았다. 제어기 범위와 시간·모드 계약 확정 후 구현한다.

### 사용자 제안과 비교 — 구현 전 논의

- 사용자 제안은 전체 horizon 전달, timestamp 기반 reference 선택/보간, 차체 속도 출력, 기존 Simulator 연결을 우선 구현하고 tracking control law는 제외하는 단계적 접근이다. 현재 요청은 이 제안과 기존 의견의 비교이며 구현 지시로 확정하지 않는다.
- 기존 제안과 방향은 일치한다. 첫 단계 출력은 추출한 reference의 vx/vy/yaw_rate를 전달하는 feedforward로 명시하고, 위치·자세 오차 보정 성능과 구분한다.
- 보완할 계약: yaw는 차체 방향, 위치/자세는 고정 좌표계, 속도는 목표 차체 축 기준. 공통 ROS clock에서 실행 시작 시각과 점별 상대 시간을 사용하며 수신 시각으로 궤적 시간을 재설정하지 않는다.
- plan_id 및 새 계획의 활성화 시각, 미래 시작 계획 대기, 지연/역순 도착과 만료 처리, 빈 궤적·시간 역행 처리 정책이 필요하다. yaw 보간은 각도 경계를 처리해야 한다.
- 기존 AllocationResult의 실행용 horizon을 내보내고 기존 샘플링 의미를 검토한다. 다른 주기의 보간이 기존 실행 결과와 일치하는지는 검증해야 한다.
- 최종 /cmd_vel 발행 주체는 Controller 한 곳으로 정하는 안을 권고한다. 기존 안전정지·모드 전환·제자리 회전 명령 경로가 유실되지 않도록 전달 방식을 함께 정한다.
- 실측 odometry에 기반한 재계획과 폐루프 tracking law는 후속 단계로 분리 가능하다. 현재 플래너의 최초 odometry만 사용하는 동작은 첫 단계에서 명시적으로 유지할 수 있다.
- 검증 후보: 서로 다른 실행 주기, 지연된 trajectory 수신, 계획 교체, yaw 경계, 궤적 만료, 기존 모드 전환/정지 경로.

검토 범위: 메시지 정의, 플래너 발행·실행·상태 갱신 코드, 시뮬레이터 구독·적분 코드. 이번에는 문서만 갱신했으며 빌드·실행 검증은 하지 않았다.

## 현재 확인한 맥락

- 프로젝트: e-Corner 차량의 로컬 모션 플래닝 PoC.
- `simp_planner/`: C++ 플래너와 메시지 정의.
- `simp_planner_tools/`: 시나리오, reference path/costmap 생성, debug plot, 통합 launch.
- 요청 당시 IDE에 열린 파일:
  - `simp_planner/simp_planner_msgs/msg/ExecutedCommand.msg`: 실행 명령과 궤적 관련 정보.
  - `simp_planner/simp_planner_msgs/msg/DriveModeState.msg`: 주행 모드 요청 및 정렬/준비 상태.
  - `simp_planner_tools/launch/simulation.launch.py`: 시뮬레이터·시나리오·플래너·디버그 노드 실행 설정.
- 위 파일은 작업 맥락으로 확인했으며, 별도의 수정 요청은 아직 없다.

## 완료 기록

### 2026-09-20 — 논문 기반 Python 궤적추종 제어기

- 기준 논문: Ao et al., Fractal Fract. 2023, 7, 121, DOI 10.3390/fractalfract7020121. 사용자 제공 PDF를 추출하고 8쪽 수식을 렌더링해 확인했다.
- 반영 범위: 식 (10)의 차체 좌표계 오차, 식 (14)의 속도 제어, 식 (16)/(18)의 리아프노프 함수·감소식. 사용자의 추가 지시에 따라 차륜 구조와 ETM/STM은 제외했다.
- `simp_tracker/`: ament_python 패키지 등록 파일, 단일 제어기 소스, 테스트 및 README 추가. ROS 메시지 외 추가 수치 라이브러리 의존성 없음.
- `tracking_controller_node.py`: 공통 ROS 시각 기반 보간, ±π yaw 보간, 차체 오차 보정, 속도 제한, plan_id/모드/좌표계/timeout 확인, 진단 토픽 발행. 기본 50 Hz, gains 3/4/2.
- `ExecutedCommand.msg`, `planner_node.cpp`: execution_state를 nominal 명령에 함께 전달해 독립 상태 토픽 수신 순서에 따른 정상/정지 명령 혼선을 피한다.
- `simulation.launch.py`, `simp_planner_tools/package.xml`: 제어기 실행 및 Planner command remap, 제어기 on/off·주기·gain 인자와 의존성 추가.
- `planar_velocity_sim_node.py`: command timeout 및 비유한 명령 방어. 제어기 중단 시 마지막 속도의 무기한 적용 방지.
- 관련 README 및 기존 `check_trajectory_topic.py`: 실행법/제한 문서화, 실제 통합 launch에서 Tracker 활성 및 cmd_vel 단일 발행 확인 추가.
- 빌드: msgs/planner/simulator/tracker/tools 5개 패키지 성공.
- 새 패키지 검증: `colcon test --packages-select simp_tracker --event-handlers console_direct+` — 23개 통과. 식 (18), 논문 figure-eight 오차 수렴(20/50/100 Hz), 후진/횡이동, 보간/만료/상태 처리를 포함한다.
- ROS 통합 검증: `check_tracking_ros.py` — 초기 x=0.3 m, y=-0.4 m, yaw=0.9 rad에서 목표 yaw=0.4 rad 횡이동 추종. 혼합 오차 norm sqrt(ex²+ey²+eθ²)이 0.7072에서 0.0003으로 감소. 모드 전환, 제자리 회전 전달, heartbeat 중단 정지, Tracker 종료 후 Simulator timeout 통과.
- 실제 Planner 통합: s_curve, Planner 100 Hz/Tracker 50 Hz에서 3개 계획·41점/4초 horizon 수신. 단일 cmd_vel 발행, 유한 추종 오차 및 Simulator 이동 확인.
- 기존 C++ core/spot_turn 테스트 2개 통과. Simulator/Tools를 직접 pytest 실행한 결과 52개 통과, 3개 실패.
- 기존 실패 3건: `test_planning_call_count_report.py`의 두 테스트(새 타이밍 필드 누락 입력), `test_spot_turn_diagnostics.py`의 모드 피드백 없는 시각화 표시 테스트. 해당 소스와 테스트가 HEAD 대비 미변경임을 확인했으며 이번 제어기 범위에서 수정하지 않았다.
- 기존 `planar_velocity_sim`/`simp_planner_tools`는 colcon이 테스트를 찾지 못해 exit 5가 발생하여 위 직접 pytest로 확인했다. 새 `simp_tracker`는 pytest 등록을 포함해 colcon 테스트가 정상 실행된다.
- 한계: 샘플링·보간·포화가 있으므로 논문 연속시간 안정성 증명을 그대로 보장하지 않는다. 큰 추종 오차의 보정 경로에 대한 충돌 검사 및 가속도·저크 제한은 후속 검토 대상이다.

### 2026-09-20 — Ponytail 기준으로 메시지 변환 통합

- `planner_node.cpp`: 한 곳에서만 사용하는 변환을 `publish_trajectory()`에 직접 통합. 기존 필드 매핑·샘플·시작 시각·발행 시점은 유지.
- `trajectory_message.hpp` 삭제. 해당 헤더 전용 `test_trajectory_message.cpp`와 CMake 테스트 타깃 제거.
- `simp_planner/simp_planner_cpp/test/check_trajectory_topic.py`: 실제 s_curve 시뮬레이션을 실행하여 궤적 시간/계획 ID/필드 및 기존 cmd_vel·odom을 확인하는 스크립트 추가. README에 실행 방법 기록.
- 검증: 메시지/플래너 빌드 성공, 기존 C++ 테스트 2개 통과, 실제 토픽 확인에서 3개 계획과 41점/4초 horizon 수신 및 Simulator 이동 확인.
- 추종 제어기는 추가하지 않았으며 기존 메시지 인터페이스와 Simulator 연결 유지.

### 2026-09-20 — 시간 궤적 메시지와 전체 horizon 발행

- `simp_planner/simp_planner_msgs/msg/TrajectoryPoint.msg`: 한 점의 위치·차체 자세·차체 속도·상대 시간·주행 모드 정의.
- `simp_planner/simp_planner_msgs/msg/Trajectory.msg`: header, plan_id, start_time, 점 배열 정의.
- `simp_planner/simp_planner_msgs/CMakeLists.txt`, `package.xml`: 두 메시지 생성 및 builtin_interfaces 의존성 등록.
- `simp_planner/simp_planner_cpp/include/simp_planner/trajectory_message.hpp`: 최종 AllocationResult를 원본 샘플과 시작 시각을 보존해 메시지로 변환.
- `simp_planner/simp_planner_cpp/src/planner_node.cpp`: 계획 활성화 시 `/planner/trajectory` 발행 추가. 기존 명령·시각화 토픽 유지.
- `simp_planner/simp_planner_cpp/test/test_trajectory_message.cpp`, `CMakeLists.txt`: 횡이동에서 차체 yaw 구분, 비균일 시간 간격, 마지막 점 포함, 지연 발행 시 시작 시각 보존 검증 등록.
- `simp_planner/README_KR.md`: 필드 의미, QoS, 발행 시점, 현재 실행 경로와의 관계 문서화.
- `작업.md`: 확정 범위와 검증 결과 기록.
- 빌드: `colcon build --symlink-install --packages-select simp_planner_msgs simp_planner_cpp --cmake-args -DBUILD_TESTING=ON` 성공.
- 테스트: `colcon test --packages-select simp_planner_cpp --event-handlers console_direct+` — core/spot_turn/trajectory_message 3개 모두 통과.
- 인터페이스: `ros2 interface show simp_planner_msgs/msg/Trajectory`로 중첩 점 배열과 start_time 생성 확인.
- 통합 확인: 별도 ROS domain에서 s_curve 시나리오 실행. 12개 계획 수신, 마지막 계획 41점/4초 horizon. plan_id 증가, 점 시간 증가, 좌표계/유한값, 기존 cmd_vel 및 이동 중 odometry 확인.
- 테스트 종료의 SIGINT 처리 중 기존 Python 노드에서 중복 `rclpy.shutdown()` 오류가 출력됨. 데이터 송수신 검증은 통과했으며 종료 처리 코드는 이번 범위에서 변경하지 않음.
- `git diff --check` 통과. Controller/Simulator/launch 동작 변경 없음.

### 2026-09-20 — 작업 관리 문서 생성

- 요청: `sum-motion-planning` 안에서 진행 사항과 해야 할 일을 관리한다.
- 변경: 저장소 루트에 `작업.md` 생성. 기록 원칙과 진행 중/할 일/완료 기록 구분.
- 확인: 저장소 README, 관련 메시지 및 launch 설정 확인. 시작 시 Git 작업 트리에 변경 없음.
- 검증: 문서 내용과 생성 경로 확인. 문서만 추가하여 빌드·시뮬레이션은 실행하지 않음.
